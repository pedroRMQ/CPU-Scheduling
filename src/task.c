#include "task.h"
#include <stdio.h>
#include <stdlib.h>

void rate(Task *tasks, int numTasks, int totalTime) {
    for(int i = 0; i < totalTime; i++) {

        for(int j = 0; j < numTasks; j++) {

            
        }
    }
}

void edf(Task *tasks, int numTasks, int totalTime) {

}